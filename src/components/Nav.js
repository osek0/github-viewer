import React from 'react';
import Container from './Container';
import Form from './From';

const Nav = () => {
  return(
    <nav>
      <Container>
        <Form />
      </Container>
    </nav>
  );
}

export default Nav;